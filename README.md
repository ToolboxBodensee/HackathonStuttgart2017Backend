# Wriggle

### Client > Server

* join (name, color)
* disconnect // we could use just the disconnect event
* changeDirection (angle)

### Server > Frontend

* joined
* left
* collision
* tick
* playerList

### Frontend > Server

* startGame
* stopGame